# OTTO 实验记录 / 消融表

> 加权 Recall@20 = 0.10·clicks + 0.30·carts + 0.60·orders
> 最后更新:2026-08-10

## ⚠️ 评估口径说明(**数不能跨口径比**)

| 口径 | co-vis 矩阵 | 验证集 | 用途 | baseline |
|---|---|---|---|---|
| **全量** | 全量(30 分块) | 全量 4.36M 本地 val | 当前主开发口径,最慢(~28min) | 0.4100 |
| **full矩阵/10万** | 全量 | 前 10 万 session 切片 | 快速隔离实验(几分钟) | 0.3288 |
| **1/6采样** | 5/30 分块 | 前 10 万切片 | 开发期秒级自测 | 0.3288 |

> 10 万切片(按 session id 取前 10 万)系统性偏低(baseline 0.3288 vs 全量 0.4100),**只在同口径内比较**。

---

## Phase 1 · baseline(recent 自身臂)

| 口径 | clicks | carts | orders | **weighted** |
|---|---|---|---|---|
| 全量 | 0.3065 | 0.2554 | 0.5046 | **0.4100** |

---

## Phase 2 Step 1 · click co-vis

| 口径 | 方法 | clicks | carts | orders | **weighted** | 备注 |
|---|---|---|---|---|---|---|
| 全量 | + click covis(**泄漏**) | 0.4621 | 0.3409 | 0.5710 | ~~0.4911~~ | ❌ 验证泄漏,虚高 |
| 全量 | + click covis(**修复**) | 0.4144 | 0.3047 | 0.5427 | **0.4585** | ✅ leak-free,**+11.8%** vs baseline |

**关键发现 — 验证泄漏(target leakage)**
`build_covis` 直接读原始 train,把验证 session 的**未来事件**(val_labels)一起数进了共现矩阵 → "用答案预测答案"。
- **修复**:新增 `build_corpus()`,语料 = 历史(ts<边界)⊕ val_input,**排除所有未来**;`popularity` 同样只数边界前。
- **影响**:泄漏在全量把 weighted 虚高 **0.0326(+6.6%)**;长尾稀有对受影响最重。修复后 lift 仍有 +11.8%,主体是真本事。

---

## Phase 2 Step 2 · buy_weighted + buy2buy + 分目标融合

**隔离实验(full矩阵 / 10万切片,同口径)——只换 carts/orders 的 co-vis 矩阵:**

| 方法 | clicks | carts | orders | **weighted** | Δweighted |
|---|---|---|---|---|---|
| all-click(对照) | 0.3869 | 0.2292 | 0.4399 | 0.3714 | — |
| buy for carts/orders | 0.3869 | 0.2301 | 0.4416 | **0.3727** | **+0.0013** |

**关键发现 — 买矩阵只有边际贡献(+0.0013,+0.35%)**
1. **buy_weighted ≈ click**:同一批源事件(`types=None`, 1h 窗口),只换加权 → 新信息有限。
2. **当时提出“硬分层可能是瓶颈”假设**:自身臂永远压 co-vis，可能让新品挤到 20 名外；后续 Step 3 已实际测试并证伪该假设。

> 结论:Step 2 重构正确、买矩阵作为 Phase 3 的独立 GBDT 特征值得保留；但买矩阵本身只有边际收益。后续松分层实验明显变差，因此“大头收益在 soft blend”这一预判已作废。

**补充(1/6采样,同口径对照)**:Step 1(全 click)0.3624 vs Step 2(分目标)0.3623 — 采样下 buy2buy 太稀(源商品仅 13 万),看不出差异;全量下才显(41 万)。

---

## Phase 2 Step 3 · 松分层实验(❌ 证伪)

**假设**:硬分层("自身臂永远压 co-vis")把 carts/orders 卡住了 —— 答案多是新品,被陈旧自身候选挤出 top-20。
**做法**:carts/orders 改软融合(两臂分数各按 session min-max 归一化 → `W_SELF·self + W_COVIS·covis` 混排),`W_SELF=W_COVIS=1`。

| 配置(full矩阵 / 10万切片) | clicks | carts | orders | **weighted** |
|---|---|---|---|---|
| 硬分层(对照) | 0.3869 | 0.2302 | 0.4416 | **0.3727** |
| 软融合 | 0.3869 | 0.1792 | 0.3637 | **0.3107** |
| recent baseline(参照) | 0.2873 | 0.1899 | **0.4052** | 0.3288 |

**结论 — 假设被证伪,已回退硬分层(`TYPE_BLEND` 全 False)**
- 软融合把 weighted 打到 **0.3107**(−0.062)。铁证:软融合 orders **0.3637 < 纯自身 baseline 0.4052** —— 比"只推最近看过的"还差。
- 说明 **OTTO 复购/复看信号极强**,自身臂对三目标都该优先;co-vis 的正确位置是"填自身用剩的空位"(硬分层只赚不亏),不是"和自身抢位"(软融合赚不抵亏)。
- clicks 稳定 0.3869(它 blend=False),证明是真效应非 bug。
- `_soft_rank` 代码保留作备选/文档。

---

## Phase 2 收尾 · 小旋钮扫描(随机 10 万代表性样本)

**先修了评估偏差**:`--sample` 从"按 id 取前 10 万"(偏样本,baseline 0.33)改为**随机采样**(baseline 0.4083 ≈ 全量 0.41),小旋钮相对排序才可信。

| 段 | 配置 | carts | orders | **weighted** |
|---|---|---|---|---|
| 参照 | recent baseline | 0.2565 | 0.5013 | 0.4083 |
| A | all-click(硬,top_n=50) | 0.3247 | 0.5507 | 0.4734 |
| A | **multi(买信号)** | 0.3257 | 0.5527 | **0.4748** |
| B | top_n=20 / 40 / 50 | — | — | 0.4737 / 0.4745 / **0.4747** |
| C | buy2buy ×1 / ×3 / ×5 | — | — | **0.4747** / 0.4745 / 0.4739 |

**结论 — 三个旋钮全边际(整轮 +0.001):**
- **买矩阵 +0.0014**(好样本上仍 marginal,证实之前不是被偏样本骗)。保留:Phase 3 当 GBDT 特征。
- **top_n=50 微赢**(20→50 仅 +0.001,单调)→ 采纳(顺带扩大 Phase 3 候选池)。已写进 `COVIS_CONFIG`。
- **buy2buy 权重 β=1 最好**,上调反而掉 → 不动。

**采纳配置**:multi + top_n=50 + buy2buy×1,随机 10 万 = **0.4748**。

---

## Phase 2 工程收尾 · 候选生成分块 + 多 K 指标

- **候选生成分块**(`generate_predictions_chunked`):按 `session % 20` 分批,各调现有
  `generate_predictions` 再拼。session 间独立 → 结果与不分块完全一致。主要价值是
  **控内存**,让 3 矩阵 + top_n=50 的全量跑得动不 OOM("分块控内存"用在 serving 端)。
  全量用时 28min → 18min(3 矩阵工作量本就大,提速次要)。
- **多 K 指标**(`evaluate_at_ks`)+ **热门依赖度上界**(`popular_share`)；后者不是 catalog coverage。

### Phase 2 最终成绩(全量本地口径 = 4.36M val,recall@20)

| 方法 | clicks | carts | orders | **weighted@20** |
|---|---|---|---|---|
| recent baseline | 0.3065 | 0.2554 | 0.5046 | 0.4100 |
| **multi-covis(最终)** | 0.4530 | 0.3227 | 0.5557 | **0.4755** |

> baseline 全量 0.4100 与 Phase 1 锁定值**完全一致** → 全流程无漂移。

### recall@K 天花板(随机 10 万代表性样本)

| K | clicks | carts | orders | **weighted** |
|---|---|---|---|---|
| @20 | 0.4534 | 0.3228 | 0.5596 | 0.4779 |
| @50 | 0.5197 | 0.3633 | 0.5901 | 0.5151 |
| **@100** | 0.5550 | 0.3932 | 0.6088 | **0.5387** |

**热门占比(top20,上界)≈ 5%** → co-vis 扛主力,几乎不靠热门凑数。

**读数**:recall@100 = **0.5387** 是 Phase 3 GBDT 的天花板 —— 精排只能在 top-100 里重排,
把 @20 的 0.4755 往 0.5387 推,**headroom ≈ +0.06**。这就是开 Phase 3 的量化理由。

### 2026-08-10 当前代码复核（同样随机 10 万）

| K | clicks | carts | orders | **weighted** |
|---|---:|---:|---:|---:|
| @20 | 0.4545 | 0.3233 | 0.5586 | **0.4776** |
| @50 | 0.5178 | 0.3618 | 0.5906 | **0.5147** |
| @100 | 0.5531 | 0.3922 | 0.6115 | **0.5399** |

本次复核与锁定实验非常接近，方向与候选 headroom 结论不变；面试文档使用本次重跑值，
上表 `0.5387` 保留为当时提交到实验日志的锁定结果。

### 2026-08-10 确定性修复回归（现有全量矩阵，连续运行两次）

| K | clicks | carts | orders | **weighted** | 两次是否一致 |
|---|---:|---:|---:|---:|---|
| @20 | 0.4545 | 0.3233 | 0.5588 | **0.4777** | 逐项一致 |
| @50 | 0.5178 | 0.3618 | 0.5906 | **0.5147** | 逐项一致 |
| @100 | 0.5531 | 0.3923 | 0.6114 | **0.5398** | 逐项一致 |

这是一项**工程回归测试，不是新增算法收益消融**：增加 aid/aid_y 次级排序键后，
同一代码、同一验证样本连续两次输出完全一致。与上一表的万分位差异来自并列项顺序被固定，
不能解释为模型能力提升或下降。

**更新(已闭环)**：随后用带 `aid_y` 兜底键的代码 `--build` 重建了三张矩阵并重跑全量 →
新的确定性 full recall@20 = **0.4755**，与锁定值**逐位一致**（clicks 0.4530 / carts 0.3227 /
orders 0.5557）。说明并列项占比极低、确定性修复**零分数影响** —— 正是理想结果
（修的是可复现，本就不该改分）。0.4755 现为"确定性 + 全量矩阵已修"的终值。

---

## 已知限制 / 遗留(Phase 2 复盘 · reviewer 5 条分诊)

- ✅ **#1 矩阵路径混用**(主入口已修):砍掉 `run_recall.py --sample` 的 1/6 矩阵捷径；
  标准入口现在只会复用或构建全量矩阵，当前三张产物行数也与全量记录一致。
  仍没有 manifest；直接调用 `build_covis(max_chunks=...)` 依然可能把部分矩阵写到同名路径。
- ✅ **#5 相同 score 缺 aid 次级键**(代码 + 产物均已修):`covis` top-N 截断、`get_seeds`、
  `_hard_rank`/`_soft_rank`、`_predict_one_type` 尾排、`baseline_recent` 全部加了
  aid/aid_y 兜底键。三张矩阵已用新代码 `--build` 重建,全量重跑 recall@20=0.4755 且可复现。
- ⏳ **#2 carts/orders 预测完全相同**:两者 `TYPE_MATRIX`/`TYPE_BLEND` 相同 →
  `_predict_one_type` 入参相同 → 输出逐字节相同(**非 bug**:recall 数不同只因答案不同)。
  carts↔orders 差异化是 Phase 3 两个独立 GBDT 的活。可选提速:算一次复用(省全量约 1/3 时间)。
- ⏳ **#3 Top-100 长表 + source/rank/score 未保存**:当前只出 top-k 的 aid 列表。
  Phase 3 需要候选长表 `[session, type, aid, 各路分数/名次/来源]` 当 GBDT 训练数据。
  → **Phase 3 Step 0:`generate_candidates_long()`**。
- ⏳ **#4 矩阵全入内存 + 最终 concat 不流式**:候选分块只降中间峰值;三张矩阵(~1.9GB)
  仍常驻,最终预测 concat 回内存。现规模够用;真正流式(每块 sink 到 parquet)与 #3
  一起打包进 Phase 3 的候选基建。

---

## ✅ Phase 2 封顶

纯规则多路召回 leak-free 全量 **recall@20 = 0.4755**(确定性 + 全量矩阵已修,已达规则天花板);
工程(分块控内存)、指标(recall@K + 热门依赖度)、全量本地数、可复现均落定。
**遗留:Kaggle 首次提交做 CV↔LB 对齐(需账号,手动);缓存 provenance/manifest、自动化测试、
Phase 3 候选长表(#2/#3/#4)留待后续。**
大跳靠 **Phase 3 GBDT 精排**(headroom +0.06 已量化)。
